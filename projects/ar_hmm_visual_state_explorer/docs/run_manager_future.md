# Future run manager

A later version should add persistent run hashes and cache selection for repeated experiments. The current app caches Yahoo downloads and stores generated outputs only when run via CLI.
