# Troubleshooting

If Yahoo Finance returns no data, check ticker suffixes and date range. Many European listings require exchange suffixes such as `.MI` or `.AS`.

If a full walk-forward is slow, start with `--max-refits 2` or use the dashboard trial setting.
